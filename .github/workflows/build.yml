name: Build Garmin Widget

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # --- 🏗️ Previous steps (keep these untouched) ---
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # --- 🧩 New jungle detection step ---
      - name: Detect jungle file
        id: detect_jungle
        run: |
          echo "🔍 Searching for .jungle files..."
          JUNGLE_FILES=$(find . -type f -name "*.jungle")
          COUNT=$(echo "$JUNGLE_FILES" | wc -l)

          if [ "$COUNT" -eq 0 ]; then
            echo "❌ No .jungle file found in repository!"
            exit 1
          elif
