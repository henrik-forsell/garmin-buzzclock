name: Build Garmin BuzzClock

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y unzip openjdk-11-jdk wget

    - name: Download and install Connect IQ SDK Manager
      run: |
        wget https://developer.garmin.com/downloads/connect-iq/sdks/connectiq-sdk-manager-linux.zip -O sdk-manager.zip
        unzip sdk-manager.zip -d sdk-manager
        chmod +x sdk-manager/sdkmanager
        ./sdk-manager/sdkmanager --install latest
        echo "SDK_PATH=$(./sdk-manager/sdkmanager --show-path latest)" >> $GITHUB_ENV

    - name: Decode developer key
      run: |
        echo "${{ secrets.DEVELOPER_KEY_B64 }}" | base64 -d > developer_key.der

    - name: Build BuzzClock widget
      run: |
        $SDK_PATH/bin/monkeyc \
          -o BuzzClock.prg \
          -m manifest.xml \
          -y developer_key.der \
          -z resources \
          -w source

    - name: Upload BuzzClock.prg artifact
      uses: actions/upload-artifact@v3
      with:
        name: BuzzClock
        path: BuzzClock.prg
