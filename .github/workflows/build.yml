name: Build BuzzClock (Signed - raw key)

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/blackshadev/garmin-connectiq-tools:8.2.3-2

    steps:
      - uses: actions/checkout@v4

      - name: Write developer key
        shell: sh
        run: |
          printf "%s" "${{ secrets.GARMIN_DEVELOPER_KEY }}" > developer_key.der

      - name: Check tool versions
        run: |
          which monkeyc
          monkeyc --version || true

      - name: Build PRG with developer key
        run: |
          monkeyc \
            -o BuzzClock.prg \
            -f manifest.xml \
            -d fenix5 \
            -y developer_key.der

      - name: Upload PRG artifact
        uses: actions/upload-artifact@v4
        with:
          name: BuzzClock-prg
          path: BuzzClock.prg
