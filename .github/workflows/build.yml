name: Build Garmin PRG

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install Connect IQ SDK
        run: |
          # Install required dependencies for the SDK Manager
          sudo apt-get update
          sudo apt-get install -y libsecret-1-0 libwebkit2gtk-4.0-37
          
          # Download and install the SDK Manager
          curl -L -o sdk-manager.zip https://developer.garmin.com/downloads/connect-iq/sdk-manager/connectiq-sdk-manager-linux.zip
          
          # Create the directory for the SDK Manager and extract the files into it
          mkdir connectiq-sdk-manager
          unzip sdk-manager.zip -d connectiq-sdk-manager
          
          # Use the SDK Manager to install a specific SDK and device files
          ./connectiq-sdk-manager/bin/sdkmanager --acceptLicense
          ./connectiq-sdk-manager/bin/sdkmanager --install --sdk-version 5.0.0 --device-api-version 4.0.0 --all-devices
    
          # Set up the path to the monkeyc compiler
          echo "CIQ_SDK_PATH=$HOME/.Garmin/ConnectIQ/Sdks/connectiq-sdk-5.0.0" >> $GITHUB_ENV
          echo "$HOME/.Garmin/ConnectIQ/Sdks/connectiq-sdk-5.0.0/bin" >> $GITHUB_PATH
    
      - name: Build Connect IQ project
        run: monkeyc -r -f monkey.jungle -y ${{ secrets.GARMIN_DEVELOPER_KEY }} -o build/BuzzClock.prg
